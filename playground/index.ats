/*
class Map[T] {
	record: Record[T] = { }

	add: (String, T) -> T = f(key, value) {
		return this.record.add(key, value)
	}
}

var stringMap = Map[String]()
stringMap.add("key", "foo")

var numberMap = Map[Number]()
numberMap.add("key", 0)
*/

type Rec = Record[Record[String] | String]

type Lis = List[List[String]]

var list: Lis = [""]